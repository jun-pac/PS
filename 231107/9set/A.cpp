#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<ll, ll> pii;
const ll MAXN = 1000010;
ll N, M, L[MAXN], D[MAXN];
vector<ll> id;
int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    
}